package com.Banking;

import java.sql.*;

public class connec {
    Connection c;
    Statement s;
    public connec(){                    // for attaching sql part before trybut it didn't worked
     /*   try {
            s = c.createStatement();
        } catch (Exception e) {
            e.printStackTrace();
        }*/
    }
}
